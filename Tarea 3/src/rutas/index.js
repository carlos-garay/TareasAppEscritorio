const router = require('express').Router();
//const rutasGustos = require('./gustos');
//const rutasMetas = require('./metas'); 
//const {auth} = require('./../middlewares');

//router.use('',auth),
//router.use('/gustos', rutasGustos);
//router.use('/usuarios', rutasMetas);

module.exports = router;